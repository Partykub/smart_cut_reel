{
  "name": "smart-cut-reel-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sh -ac 'if [ -f ./.env.local ]; then . ./.env.local; fi; next dev --port ${FRONTEND_PORT:-3000}'",
    "build": "next build",
    "start": "sh -ac 'if [ -f ./.env.local ]; then . ./.env.local; fi; next start --port ${FRONTEND_PORT:-3000}'",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "next": "^15.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.13",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}