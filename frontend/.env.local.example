# Base URL of the Smart Cut Reel orchestrator API.
# Used only on the server side (Next.js route handlers).
ORCHESTRATOR_BASE_URL=http://localhost:8000

# Optional frontend port used by npm run dev / npm start.
# FRONTEND_PORT=3000
